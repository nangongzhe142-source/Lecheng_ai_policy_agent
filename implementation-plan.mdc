# 博鳌乐城政策问答 AI Agent — 实施计划

> 本文件记录项目实施进度，每完成一个步骤必须追加 `Done` 和两行完成总结。
> 严格遵循顺序执行，不得跳步。

---

## 执行原则

- 每次只处理一个模块
- 先接口协议，再代码实现
- 每步完成后追加 Done + 两行总结
- 不删除无关代码，不破坏 Dify 原有功能
- 所有政策回答必须有可验证来源

---

## 阶段一：全局规划

### 步骤 1.1：参考网页分析 + 仓库结构确认
- 状态：Done
- 完成总结：已获取 regai.world/compare 页面为 React SPA（Global Regulatory AI Platform），确认布局为政策对比型政务 AI 平台。
- 已确认 Dify 仓库根目录结构：api/、web/、docker/、sdks/、packages/、e2e/、docs/、dev/、.github/，版本 1.14.0。

### 步骤 1.2：全局规划文档输出
- 状态：Done
- 完成总结：已创建 docs/lecheng-agent/project-rules.md，包含项目目标、用户角色（9类）、核心业务场景（11类）、总体架构、Dify 复用边界、新增模块边界。
- 已输出 Mermaid 架构图、模块职责表、分阶段实施路线（4期12周）。

---

## 阶段二：项目规则文件

### 步骤 2.1：创建 docs/lecheng-agent/project-rules.md
- 状态：Done
- 完成总结：包含禁止无来源回答、禁止医疗诊断建议、政策解释三层区分（原文/解释/建议）等核心规则。
- 覆盖日志脱敏、租户隔离、接口协议、幂等策略等工程规则共 20 条。

### 步骤 2.2：创建 docs/lecheng-agent/module-contracts.md
- 状态：Done
- 完成总结：定义 10 个模块的输入输出契约，每个模块含职责、边界、调用方、被调用方、错误码、测试方式。
- 覆盖政策采集、知识库构建、检索、问答生成、合规校验、多轮对话、UI、管理后台、评测、部署运维。

### 步骤 2.3：创建 docs/lecheng-agent/interface-contracts.md
- 状态：Done
- 完成总结：定义 12 条模块间接口协议，每条含请求/响应数据结构、错误码、超时、重试、幂等性、租户隔离、审计字段。
- 覆盖 PolicyDocumentNormalized、RetrievalRequest/Result、PolicyAnswer、ComplianceCheckResult 等核心 TypeScript interface。

### 步骤 2.4：创建 docs/lecheng-agent/module-dependency-map.md
- 状态：Done
- 完成总结：输出模块依赖 Mermaid 图，明确正向调用链和反向依赖关系。
- 标注每条依赖边的调用方式（函数调用/HTTP/Celery/WorkflowNode/Hook）。

### 步骤 2.5：创建 docs/lecheng-agent/error-code-spec.md
- 状态：Done
- 完成总结：定义 LC- 前缀错误码体系，覆盖 1000-5999 段，含采集、检索、问答、合规、认证、系统六大类。
- 每个错误码含 HTTP 状态码、可重试标志、用户提示文案、内部日志级别。

### 步骤 2.6：创建 docs/lecheng-agent/security-rules.md
- 状态：Done
- 完成总结：定义认证鉴权、租户隔离、API Key 管理、文件上传安全、SSRF 防护、Prompt Injection 防护等 14 条安全规则。
- 包含日志脱敏字段列表、审计日志必填字段、数据备份与版本追踪要求。

### 步骤 2.7：创建 docs/lecheng-agent/policy-answer-rules.md
- 状态：Done
- 完成总结：定义政策问答生成的 15 条强制规则，包含三层解释区分、引用格式、拒答触发条件。
- 包含 9 类禁止输出场景和回答质量评分标准。

### 步骤 2.8：创建 docs/lecheng-agent/testing-rules.md
- 状态：Done
- 完成总结：定义单元/API/E2E/RAG质量/合规拒答/权限隔离等 10 类测试规范。
- 覆盖 10 项评测指标（准确率/引用命中率/幻觉率等）及自动化评测流水线设计。

---

## 阶段三：模块划分与接口协议

### 步骤 3.1：核心数据模型设计
- 状态：Done
- 完成总结：设计 PolicyDocument、PolicyChunk、PolicySource、PolicyAnswer 等 13 个核心数据模型，每个模型含字段定义、索引、租户隔离、审计字段。
- 提供 SQLAlchemy 模型骨架和 TypeScript interface 对照，明确表关系。

### 步骤 3.2：Dify 集成方案确认
- 状态：Done
- 完成总结：确认复用 Dify Dataset/RAG、Workflow Engine、App API、权限系统，新增 lecheng_policy 蓝图 (controllers/lecheng_policy/)。
- 明确不修改 Dify 核心代码的边界，所有新增以独立蓝图、新 Service、新 Task 形式接入。

---

## 阶段四：后续实现（已完成）

### 步骤 4.1：政策资料采集模块骨架实现
- 状态：Done
- 完成总结：已创建 api/models/lecheng_policy.py（13 个数据模型，含全部枚举、索引、外键、租户隔离、审计字段），并追加到 api/models/__init__.py 的 imports 和 __all__。
- 已创建 Alembic 迁移文件（lc0001lecheng01，down_revision=a4f2d8c9b731），创建 lecheng_policy 蓝图（api/controllers/lecheng_policy/）并追加注册到 ext_blueprints.py，实现健康检查接口 GET /api/lecheng/health，所有文件语法检查和 lint 通过，单元测试覆盖枚举值、表名前缀、租户隔离字段、模型默认值共 5 个测试类。

### 步骤 4.2：政策知识库构建模块实现
- 状态：Done
- 完成总结：已实现 `LechengPolicyDocumentService.submit_kb_build_job` / `build_kb`，通过 `FileService.upload_text` + `DocumentService.save_document_with_dataset_id` + `DocumentIndexingTaskProxy` 写入 Dify Dataset 并派发索引任务；新增 Celery 任务 `lecheng_policy_kb_build_task`（`dataset` 队列）并在 `ext_celery.py` 注册 import。
- 管理端接口：`POST /api/lecheng/admin/documents/{id}/rebuild-kb`、`GET .../kb-status`（`setup_required` + `login_required`）；错误码对齐 LC-2007/LC-2001/LC-1008 等；单元测试 `test_policy_indexing.py`。

### 步骤 4.3：检索模块配置
- 状态：Done
- 完成总结：新增 `api/services/lecheng/retrieval_config.py`（默认 top_k/score_threshold/混合权重/超时/最短查询长度）与 `policy_retrieval_service.py`（`PolicyRetrievalService.retrieve` 基于 `DatasetRetrieval.knowledge_retrieval` multiple 模式，`LechengRetrievalRequest/Result` Pydantic 模型，Dataset ID 解析、乐城元数据过滤、LC-3001～3005）。
- `services/lecheng/__init__.py` 改为懒加载避免子模块导入链；单元测试 `tests/unit_tests/lecheng/test_policy_retrieval.py`（Python 3.11 以下模块级 skip，与 Dify 运行版本一致）。

### 步骤 4.4：政策问答 Workflow 配置
- 状态：Done
- 完成总结：新增 `policy_workflow_config.py`（变量名 `question/scene/user_role/conversation_history/retrieval_chunks`、输出键 `policy_answer`、免责与超时常量），`policy_qa_workflow_io.py`（chunk 序列化、Workflow 字符串入参、outputs 解析与 LC-4003/4004/4005），`policy_qa_service.py`（`AnswerGenerationRequestModel` 与 `PolicyQAService.build_workflow_inputs` / `parse_and_validate_workflow_answer`）。
- 单元测试 `test_policy_qa_service.py` 共 11 条在 Python 3.10 下通过（`PYTHONPATH=api`、无 Dify 核心依赖）；`services/lecheng/__init__.py` 已懒加载导出 M04 符号。

### 步骤 4.5：合规校验模块实现
- 状态：Done
- 完成总结：已新增 `api/core/lecheng/compliance_checker.py`（`ComplianceChecker.check`、`ComplianceCheckRequest/Result`、LC-5001/5002 异常、内置规则与 `map_rule_id_to_lc_rejection_code`），扫描正文仅限 `content` 字段以免固定免责声明误触医疗规则。
- 已新增 `api/core/lecheng/__init__.py` 与 `api/tests/unit_tests/lecheng/test_compliance_checker.py`（15 条用例，`pytest --noconftest` 通过）。

### 步骤 4.6：多轮对话模块实现
- 状态：Done
- 完成总结：已新增 `conversation_workflow_config.py`（Workflow 03 变量名、`lecheng:session:{id}:history` 与 TTL 2h）与 `conversation_service.py`（`ConversationService.rewrite`、Redis 读写、`build_rewrite_workflow_inputs` / `parse_rewrite_workflow_output`、LC-6001～6003）。
- 已新增 `test_conversation_service.py` 共 14 条用例（`patch` 替换 `_redis` 桩，避免部分环境导入 `ext_redis` 失败），`pytest --noconftest` 通过；`services/lecheng/__init__.py` 懒加载导出 M06 符号。

### 步骤 4.7：前端政策问答页面实现
- 状态：Done
- 完成总结：已按 `docs/lecheng-agent/dify-integration.md` 增加 `web/app/(commonLayout)/lecheng/`（layout、首页重定向、`qa` 与 `compare` 占位页）、组件（`PolicyChatPanel`、`CitationPanel`、`RiskDisclaimer`、`SceneSelector`）、`store/atoms.ts`，以及 `web/types/lecheng.ts`、`web/types/lecheng-ui.ts` 与 `web/service/lecheng/`（`getLechengApiBaseUrl`、`postPolicyQaNonStream`、`usePolicyQaMutation`）。
- 已添加模块检测：`web/service/lecheng/api.spec.ts`、`lecheng/components/risk-disclaimer.spec.tsx`；在 `web` 目录安装依赖后可执行 `pnpm test -- service/lecheng/api.spec.ts` 与 `pnpm test -- lecheng/components/risk-disclaimer.spec.tsx`（或等价 vitest 路径）。

### 步骤 4.8：管理后台模块实现
- 状态：Done
- 完成总结：已落地 `admin_validation.py`（无 models 链校验 + multipart metadata）与 `LechengAdminService` 文档 CRUD/审核/审计；蓝图 `admin.py` 沿用 owner/admin 与 LC-7001/7xxx 错误约定。
- 已新增 `web/app/(commonLayout)/lecheng/admin/`（入口、文档列表与审核按钮、审计日志）、`service/lecheng/api.ts` 管理端方法与 `use-admin.ts`；`test_admin_service.py` 10 条与 `api.spec.ts` 管理端 URL 用例可在无完整 API venv 下执行。

### 步骤 4.9：评测模块实现
- 状态：Done
- 完成总结：已新增 `policy_evaluation_scoring.py`（LC-9001 用例校验、stub 对齐期望、批次指标与验收阈值）与 `policy_evaluation_service.py`（写入 `lecheng_evaluation_results`、可选 `run_inline` 或 Celery）；`policy_evaluation_task` 注册于 `pipeline` 队列并在 `ext_celery.py` 中导入。
- 已暴露 `POST/GET /api/lecheng/evaluation/jobs`；`tests/unit_tests/lecheng/test_evaluation_service.py` 8 条仅依赖打分模块，可在无 Flask-SQLAlchemy 环境下执行。

### 步骤 4.10：部署运维配置
- 状态：Done
- 阶段 1 完成总结：文档冻结与契约对齐已在仓库落地；已冻结并修订 `docs/lecheng-agent/deployment.md`、`deployment-operations.md` 及契约类文档（队列 `dataset`/`pipeline`、评测路由、M01=`admin.py`），与 `entrypoint.sh` / 任务代码一致。
- 阶段 2 完成总结：已新增 `docker/envs/core-services/lecheng.env.example`，在 `docker-compose-template.yaml` 为 api/worker 追加 `lecheng.env`（`required: false`），运行 `generate_docker_compose` 重新生成 `docker-compose.yaml`；根目录 `Makefile` 增加 `regenerate-docker-compose`、`prepare-lecheng-demo-env`，`prepare-docker` 前置执行 compose 生成；`.gitignore` 忽略物化 `docker/envs/**/*.env` 与 `docker/.env`。
- 阶段 2 完成总结：已增加 `api/tests/unit_tests/lecheng/test_lecheng_docker_env_example.py`（2 条用例）；已同步 `deployment-operations.md`、`lecheng-usage-and-operations.md` §2.3/§2.5/§10、`dify-integration.md` 目录树。

### 步骤 4.11：政策问答 HTTP（`/api/lecheng/qa`）
- 状态：Done
- 完成总结：已落地 `controllers/lecheng_policy/qa.py` 与 `policy_qa_api_service.py`：租户内检索 → `PolicyQAService.build_workflow_inputs` → `AppGenerateService.generate`（blocking）→ 解析 `policy_answer`；配置经 `LechengPolicyConfig`/`dify_config` 读取。
- 完成总结：已增加 `api/.env.example` 样例键、`docs/lecheng-agent/dev-prototype-seed.md` 与 `test_policy_qa_api_service.py`；`deployment.md` 草案与 `deployment-operations.md` §0.1/§0.2 已同步 D-01/D-06 结论。

---

## 阶段五：验收与运维收口

### 步骤 5.1：《总录》与 implementation-plan 对照验收
- 状态：Done
- 完成总结：已以 `docs/lecheng-agent/lecheng-usage-and-operations.md` 为真源核对 §1～§10 与仓库路径、命令、HTTP 行为一致；已新增该文档 §9 可勾选「网页 DEMO 验收」表并与 §5 流程、`dev-prototype-seed.md` 交叉引用。
- 完成总结：已核对阶段一至阶段四全部步骤（1.1–1.2、2.1–2.8、3.1–3.2、4.1–4.11）文面均为 **Done** 且具备完成总结与变更记录行；已修正阶段四标题与 `deployment-operations.md` §3 阶段 2 状态、§4 变更表损坏行及文首版本说明。

### 步骤 5.2：公网部署与网关（HTTPS）
- 状态：Done
- 完成总结：已在 `docs/lecheng-agent/deployment.md` 增补「公网访问与网关」表并修订 §2 与 `lecheng.env.example` 现状说明；已在 `lecheng-usage-and-operations.md` 增补 **§2.6**、**§3** 公网提示与 **§9** 公网勾选行；已在 `dify-integration.md` 二节末增加公网 `NEXT_PUBLIC_*` 与 `/api/lecheng` 一致说明。
- 完成总结：已增加 `api/tests/unit_tests/lecheng/test_lecheng_docker_env_example.py` 中公网文档节标题守护用例；实施不包含替换团队真实域名/证书；运维按文档配置 Nginx 与 `CONSOLE_WEB_URL` 等后执行《总录》§9 自外网验收。

---

## 变更记录

| 日期 | 步骤 | 操作 | 说明 |
|------|------|------|------|
| 2026-05-11 | 1.1 | 创建 | 仓库结构确认与参考网页分析 |
| 2026-05-11 | 1.2-2.8 | 创建 | 全套规划与规则文件 |
| 2026-05-11 | 4.2 | 完成 | 知识库构建服务、Celery 任务、管理端 rebuild-kb/kb-status、单测 |
| 2026-05-11 | 4.3 | 完成 | 检索默认配置、PolicyRetrievalService、test_policy_retrieval |
| 2026-05-11 | 4.4 | 完成 | Workflow 变量配置、policy_qa_service、test_policy_qa 11 passed |
| 2026-05-11 | 4.5 | 完成 | core/lecheng 合规模块、test_compliance_checker 15 passed |
| 2026-05-11 | 4.6 | 完成 | conversation_service、Redis 会话、Workflow 改写 I/O、test_conversation 14 passed |
| 2026-05-11 | 4.7 | 完成 | lecheng 前端路由与 QA UI、service/types、api/risk-disclaimer vitest |
| 2026-05-11 | 4.8 | 完成 | admin_validation、admin_service/控制器、前端 admin 页、pytest 10 + vitest api.spec 扩展 |
| 2026-05-11 | 4.9 | 完成 | policy_evaluation_scoring/service、evaluation 路由、Celery policy_evaluation_task、test_evaluation 8 passed |
| 2026-05-12 | 4.10 | Done 阶段2 | `lecheng.env.example`、compose 模板/env_file、`docker-compose.yaml` 再生；Makefile `regenerate-docker-compose`/`prepare-lecheng-demo-env`/`prepare-docker` 前置生成；`.gitignore` 物化 env；单测 `test_lecheng_docker_env_example.py`；文档 §同步 |
| 2026-05-11 | 4.11 | 完成 | `qa.py`、policy QA 编排、`LechengPolicyConfig`、env 样例、dev-prototype-seed、单测；D-01/D-06 文档更新 |
| 2026-05-12 | 5.1 | Done | 《总录》§9 验收表、`deployment-operations.md` §3/§4 与文首版本、阶段四标题修正；全文步骤 Done 核对 |
| 2026-05-13 | 5.2 | Done | `deployment.md` 公网与网关、`lecheng-usage` §2.6/§9、`dify-integration` 公网说明；`test_lecheng_docker_env_example.py` 文档标题守护；不含替运维写真实域名 |
